# convertbetcodes.com API key — enables real booking code generation
# Get your key at https://convertbetcodes.com
# Without this, the app shows "Add selections on [Platform]" links instead of codes.
CONVERTBET_API_KEY=your_api_key_here
